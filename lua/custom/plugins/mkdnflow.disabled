return {
  'jakewvincent/mkdnflow.nvim',
  enabled = false,
  config = function()
    require('mkdnflow').setup {
      -- Config goes here; leave blank for defaults
      mappings = {
        MkdnToggleToDo = { { 'n', 'v' }, 'ttd' },
      },
    }
  end,
}
